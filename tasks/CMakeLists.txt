# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(dso_slamTaskLib)
ADD_LIBRARY(${DSO_SLAM_TASKLIB_NAME} SHARED 
    ${DSO_SLAM_TASKLIB_SOURCES})
add_dependencies(${DSO_SLAM_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${DSO_SLAM_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${DSO_SLAM_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DSO_SLAM_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DSO_SLAM_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${DSO_SLAM_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${DSO_SLAM_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DSO_SLAM_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DSO_SLAM_TASKLIB_HEADERS}
    DESTINATION include/orocos/dso_slam)

